# Reverse a String

Reverse a string with the `rev` command.

```shell
$ echo 'test' | rev
tset
```

It also works with files.

```shell
$ cat Procfile | rev
br.amup/gifnoc C- amup cexe eldnub :bew
```
